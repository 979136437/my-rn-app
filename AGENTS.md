# 项目规则

## 适用范围

- 子包架构与命名规则适用于本项目维护的 `packages/*` workspace 子包，不适用于第三方依赖、外部引入的源码、示例应用或生成目录本身。子包使用生成的原生绑定，仍属于该子包的原生接入。
- 本文件补充全局工作规则，不构成 Git 提交、代码测试、项目构建或发布授权；相关操作继续遵守全局规则及用户明确指令。

## 子包原生架构与命名

- 包含原生代码的子包统一采用 `react-native-nitro-modules` 接入原生能力，并命名为 `react-native-nitro-*`；`*` 为非空功能名称，例如 `react-native-nitro-list`。
- 实际接入要求：至少一项有业务用途的原生能力通过 Nitro API 或绑定暴露或控制。仅声明依赖、添加空 HybridObject、无用途的转发或探测接口，不视为符合要求。
- 原生视图可以使用 Fabric 负责渲染；Fabric 与 Nitro 的职责应清晰，不为满足规则重复实现同一能力。无法合理采用 Nitro 的原生子包，应说明原因并由用户明确决定例外，不自行添加形式上的接入。
- 子包自身直接使用 Nitro API 或绑定时，同样必须采用上述命名前缀。仅间接依赖其他 Nitro 子包，不视为自身使用 Nitro；不包含原生代码且未直接使用 Nitro 的纯 JS/TS 子包无此前缀要求。

## 新包与存量迁移

- 新建原生子包、为纯 JS/TS 子包新增原生代码，或为子包首次直接引入 Nitro 能力时，必须同时满足架构与命名要求。
- 规则生效时已存在但不符合要求的子包属于待迁移项。日常文档、修复或功能修改不得自动扩大为整包架构迁移；发现不符合时说明情况，迁移作为用户明确要求的任务执行。
- 执行迁移或改名时，同步检查并更新包名、目录名、workspace 依赖、锁文件、导入路径、原生接入配置及文档引用，保留无关的用户修改。

## 代码生成与静态检查

- 为当前已授权改动所必需的代码生成（例如 Nitrogen、Fabric codegen）可以执行；执行前检查命令及前置脚本，确认不会间接触发原生编译、项目打包或其他受限操作。生成文件应由生成器更新，不手工修改。
- 允许代码阅读、差异检查，以及不执行被测代码、不输出编译产物的静态检查（例如 `tsc --noEmit`）。这些操作不构成代码测试或项目构建授权。
- 原生编译、打包及其他项目构建仍须用户明确要求。若代码生成或检查命令依赖构建，先完成不依赖构建的工作，再说明阻碍；不得擅自跳过或禁用必要钩子。
