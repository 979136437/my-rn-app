///
/// HybridPickerControllerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `PickerConfig` to properly resolve imports.
namespace margelo::nitro::picker { struct PickerConfig; }
// Forward declaration of `PickerEvent` to properly resolve imports.
namespace margelo::nitro::picker { struct PickerEvent; }

#include "PickerConfig.hpp"
#include "PickerEvent.hpp"
#include <functional>

namespace margelo::nitro::picker {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `PickerController`
   * Inherit this class to create instances of `HybridPickerControllerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPickerController: public HybridPickerControllerSpec {
   * public:
   *   HybridPickerController(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPickerControllerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPickerControllerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPickerControllerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void connect(double viewTag, const PickerConfig& config, const std::function<void(const PickerEvent& /* event */)>& callback) = 0;
      virtual void configure(const PickerConfig& config) = 0;
      virtual void step(double delta) = 0;
      virtual void stop() = 0;
      virtual void disconnect() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "PickerController";
  };

} // namespace margelo::nitro::picker
