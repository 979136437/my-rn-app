///
/// HybridNitroListControllerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ListSnapshot` to properly resolve imports.
namespace margelo::nitro::nitrolist { struct ListSnapshot; }
// Forward declaration of `ListConfig` to properly resolve imports.
namespace margelo::nitro::nitrolist { struct ListConfig; }
// Forward declaration of `ListItem` to properly resolve imports.
namespace margelo::nitro::nitrolist { struct ListItem; }
// Forward declaration of `ScrollMetrics` to properly resolve imports.
namespace margelo::nitro::nitrolist { struct ScrollMetrics; }

#include <string>
#include "ListSnapshot.hpp"
#include <functional>
#include "ListConfig.hpp"
#include "ListItem.hpp"
#include <vector>
#include "ScrollMetrics.hpp"
#include <NitroModules/Promise.hpp>

namespace margelo::nitro::nitrolist {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroListController`
   * Inherit this class to create instances of `HybridNitroListControllerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroListController: public HybridNitroListControllerSpec {
   * public:
   *   HybridNitroListController(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroListControllerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroListControllerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroListControllerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void connect(const std::string& listId, const std::function<void(const ListSnapshot& /* snapshot */)>& onSnapshot) = 0;
      virtual void configure(const ListConfig& config) = 0;
      virtual void setItems(const std::vector<ListItem>& items) = 0;
      virtual void setRefreshing(bool refreshing) = 0;
      virtual void resolveEndReached(double requestId, bool accepted) = 0;
      virtual void reportMeasurement(const std::string& slotId, double token, double version, double width, double height) = 0;
      virtual void scrollToOffset(double offset, bool animated) = 0;
      virtual void scrollToEnd(bool animated) = 0;
      virtual void scrollBy(double deltaY, bool animated) = 0;
      virtual void scrollToItem(const std::string& key, bool animated, const std::string& align, double offset, bool avoidHeaders) = 0;
      virtual void stopScroll() = 0;
      virtual std::shared_ptr<Promise<ScrollMetrics>> getScrollMetrics() = 0;
      virtual void disconnect() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroListController";
  };

} // namespace margelo::nitro::nitrolist
